services:
  - type: web
    name: interview-review
    env: python
    plan: free
    rootDir: backend
    buildCommand: "./build.sh"
    startCommand: "gunicorn backend.wsgi:application --workers 3 --timeout 120 --access-logfile -"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
